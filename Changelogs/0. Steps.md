````
### Step 1: Initial App Setup

1. Download the latest version of the app, open it in Android Studio, and place the `google-services.json` file inside the `android/app/` folder.

2. Change the package name by running the following command:

   ```bash
   dart run change_app_package_name:main com.bissow.app
````

Please refer to **`1. Logo and Package Name Changed.md`** for detailed instructions.

3. Change the app name in `android/app/src/main/AndroidManifest.xml`.
   Update:

   ```
   android:label="eClassify"
   ```
    to:

   ```
   android:label="Bissow"
   ```
    Also, update the URLs used for deep linking inside the relevant `<intent-filter>` sections.
4. Change the splash screen/app name in `lib/app/config/app_config.dart`.
   Update:

   ```
   static const String applicationName = 'eClassify';
   ```
   to:

   ```
   static const String applicationName = 'Bissow';
   ```
5. Change the **Admin Panel URL** and **Website URL** according to the new application configuration.
6. Change the app icons using the automatic method explained in **`2. App Icon Setup.md`**.
   As of **07.09.2026**, use the latest package version:

   ```
   flutter_launcher_icons: ^0.14.4
   ```
   Make sure all required app icon images have been added to the:

   ```
   assets/images/
   ```
   folder before running the icon generation process.
7. For Firebase configuration, refer to **`3. Firebase Related and Google Map API.md`** for detailed instructions.
   For the Firebase Android setup, make sure the `google-services.json` file is placed inside:

   ```
   android/app/
   ```
8. Don't forget to add/update the required branding images, such as:

   ```
   assets/icons/branding/company_logo.svg
   assets/icons/branding/logo.svg
   assets/icons/branding/placeholder.svg
   ```

### Final Verification

After completing all the above changes, make sure to run the app on a **real Android device**.

Verify that:

- The app name is displayed correctly as **Bissow**.
- The package name has been changed correctly.
- The app icons and splash screen are displayed correctly.
- Firebase is working properly.
- Deep linking URLs are configured correctly.
- The Admin Panel URL and Website URL are working correctly.
- All branding images are displayed correctly.
- There are no crashes or major issues.

If everything looks fine on the real device, proceed to **Step 2**.
